import { BaseLoader } from "./base_loader";
/**
 * ts模块loader加载
 */
export declare class TSLoader extends BaseLoader {
    /**
     * 就是个触发器没有实质性作用(暂时)
     */
    scriptLoader(jsObject: Object[]): void;
}
